function sumUp(n) {
    let result = 0;
    for (let i = 1; i <=n; i++) {
        result = result + i;
    }
    return(result);
}

// Simplified way to perform the same function: 
// function sumUp(n) {
//     return (n/2) * (1+n);
// })


